let userName:string[] = ["Waheed","Noman","Jaweed","Shahmeer","Kamran","Admin"]

if (userName.length === 0) {
    console.log("We need to find some users!");
    
}else {
    userName = []
    console.log("All users have been removed " + userName.length);
    
}