"use strict";
// Calculates the absolute difference between -20 and 20
const difference = Math.abs(-20 - 20);
console.log(difference); // Outputs: 40
// This line shows the absolute value of the difference between -20 and 20, ignoring the sign.
