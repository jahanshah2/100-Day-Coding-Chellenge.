"use strict";
function convertCase(inputstr) {
    let uppercase = inputstr.toUpperCase();
    let lowercase = inputstr.toLowerCase();
    console.log(`Uppercase: ${uppercase}, Lowercase: ${lowercase}`);
}
convertCase("Jahan Shah");
